apiVersion: v1
kind: Service

metadata:
  name: smart-energy-insights-service

spec:
  type: NodePort

  selector:
    app: smart-energy-insights

  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30001